# URL pública del frontend (para CORS estricto en el servidor).
# En Vercel: la URL de tu proyecto. En local: déjala vacía.
# FRONTEND_URL=https://arcade-de-juegos.vercel.app

# URL de la API cuando el frontend estático vive separado del servidor
# (Vercel/GitHub Pages + API en Render/Hugging Face).
# En el proyecto client: crea client/.env con:
# VITE_API_URL=https://tu-api.onrender.com
